# MLflow tracking configuration.
# Copy this file to `.env` and adjust as needed (not loaded automatically;
# export these variables in your shell, or use `python-dotenv` / `make`).

MLFLOW_TRACKING_URI=sqlite:///mlflow.db

# Overrides the experiment name defined in configs/*.yaml when set.
MLFLOW_EXPERIMENT_NAME=
